% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecb_exchange_rate.R
\name{ecb_exchange_rate}
\alias{ecb_exchange_rate}
\title{Euro exchange rates}
\usage{
ecb_exchange_rate(
  currency = "USD",
  frequency = c("monthly", "daily"),
  from = NULL,
  to = NULL,
  cache = TRUE
)
}
\arguments{
\item{currency}{Character vector of ISO 4217 currency codes (e.g. \code{"USD"},
\code{"GBP"}). Use \code{\link[=list_exchange_rates]{list_exchange_rates()}} to see available currencies.}

\item{frequency}{One of \code{"monthly"} (default) or \code{"daily"}.}

\item{from}{Optional start date.}

\item{to}{Optional end date.}

\item{cache}{Logical. Use cached data if available (default \code{TRUE}).}
}
\value{
A data frame with columns:
\describe{
\item{date}{Date.}
\item{currency}{Character. ISO currency code.}
\item{value}{Numeric. Units of foreign currency per euro.}
}
}
\description{
Returns ECB reference exchange rates for one or more currencies against the
euro.
}
\examples{
\donttest{
op <- options(readecb.cache_dir = tempdir())
ecb_exchange_rate("USD", from = "2024-01")
ecb_exchange_rate(c("USD", "GBP", "JPY"), from = "2024-01")
options(op)
}
}
\seealso{
Other exchange rates: 
\code{\link{list_exchange_rates}()}
}
\concept{exchange rates}
